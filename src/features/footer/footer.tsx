import ContactForm from '../contact-form';
import { FooterContainer, FooterContent } from './footer.styles';

export const Footer = () => {
  return (
    <FooterContainer>
      <FooterContent>
        <ContactForm />
      </FooterContent>
    </FooterContainer>
  );
};
